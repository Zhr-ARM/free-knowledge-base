# ROS

这个栏目用于整理 ROS 与机器人方向资料。

## 推荐内容方向

- ROS1、ROS2 环境安装与工作空间配置
- 节点、话题、服务、动作、参数和 launch 文件
- URDF、Xacro、TF、RViz、Gazebo 仿真
- Navigation、MoveIt、SLAM、机器人定位与建图
- 激光雷达、深度相机、IMU、底盘和机械臂项目实践

## 文件放置建议

把 ROS 相关资料放入：

```text
uploads/ROS/
```

运行 `npm run sync:uploads` 后，它们会出现在资料库的“ROS”分类下。
