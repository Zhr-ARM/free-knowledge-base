{
  "name": "free-knowledge-base",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:uploads": "node scripts/sync-uploads.mjs",
    "docs:dev": "npm run sync:uploads && vitepress dev docs --host 0.0.0.0",
    "docs:build": "npm run sync:uploads && vitepress build docs",
    "docs:preview": "vitepress preview docs --host 0.0.0.0",
    "docs:deploy": "cross-env VITEPRESS_BASE=/ npm run docs:build && gh-pages -d docs/.vitepress/dist"
  },
  "devDependencies": {
    "@lucide/vue": "^1.28.0",
    "cross-env": "^10.1.0",
    "gh-pages": "^6.3.0",
    "jszip": "^3.10.1",
    "mammoth": "^1.12.0",
    "pdfjs-dist": "^6.2.108",
    "vitepress": "^1.6.4",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  }
}
