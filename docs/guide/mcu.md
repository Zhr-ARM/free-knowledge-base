# 单片机

这个栏目用于整理单片机与嵌入式入门资料。

## 推荐内容方向

- C 语言基础与嵌入式常用写法
- STM32、51、ESP32、Arduino 入门
- GPIO、定时器、中断、ADC、PWM、DMA
- UART、I2C、SPI、CAN 等通信协议
- 传感器、舵机、电机、屏幕和常用模块
- Keil、STM32CubeMX、PlatformIO、串口工具和调试工具

## 文件放置建议

把单片机相关资料放入：

```text
uploads/单片机/
```

运行 `npm run sync:uploads` 后，它们会出现在资料库的“单片机”分类下。
